# Galinstan Cyber-Physical Controller: 借助视觉反馈的液态金属数字驱动平台

> **“将连续的物理流场离散化为数字信号，再将逻辑决策还原为物理界面的定向运动。”**
> 本项目利用改装的 3D 打印平台与计算机视觉，构建了一个针对 NaOH 溶液中液态金属（Galinstan）电毛细效应（Electrocapillarity）的软硬软闭环控制系统。

---

## 1. 核心哲学与物理基石 (Core Philosophy & Physics)
本项目拒绝纯粹的“黑盒调参”，坚持尽可能地（根据本人当前的能力）以流体力学第一性原理为驱动：
* **物理内核**：根据 Lippmann 方程与 Marangoni 流动原理，将外部电压转化为界面张力梯度。
* **工程架构**：采用感知-决策-执行（Sense-Think-Act）解耦架构，实现底层硬件与高层控制律的物理隔离。
* **开发原则**：最小化精力耗散，通过此项目，最大化地学习开发范式。

## 2. 系统架构 (System Architecture)
系统由四个高度解耦的模块构成，确保接口稳定性与后续算法的可延展性：

* **[Init] 边界驱动层 (`/drivers`)**：独立于核心业务逻辑，封装底层硬件（摄像头、3D 打印机步进电机、Arduino 电压输出）的初始化约束。
* **[Sense] 视觉感知层 (`src/vision`)**：基于 OpenCV 的连续时域观测器。将物理空间的溶液与液滴，降维映射为状态空间的坐标矩阵 $(x, y, t)$。
* **[Think] 动力学中枢 (`src/analysis`)**：物理引擎层。包含静态拟合公式与动态误差计算 $e(t) = x_{target} - x(t)$，将标量偏差转化为矢量电压分配。
* **[Act] 物理执行层 (`src/control`)**：通过串行通讯将数字逻辑映射为物理电极的 PWM 电压阵列。

## 3. 知识资产树 (Directory Structure)
> *注：历史实验废案与迭代碎片（原 `_old_code`）已通过 Git 版本控制进行降熵处理，不再体现于物理目录中。*

├── README.md              # 项目白皮书与架构说明
├── environment.yml        # Conda 'fluid' 运行环境镜像
├── /docs                  # 理论推导、文献链接与 Obsidian 知识库接口
├── /drivers               # 传感器与执行器的硬件接口封装
├── /src                   # 核心逻辑源码
│   ├── vision/            # 动态追踪与滤波算法
│   ├── analysis/          # 电驱动力 $F_e$ 与粘性阻力 $F_v$ 的受力方程模型
│   ├── control/           # 闭环控制律（P/PI控制）与串口通信
│   └── ui/                # Streamlit 交互式参数寻优前端 (面向测试解耦)
└── tests/                 # 各模块独立测试用例

## 4. 演进路线图 (Evolution Roadmap)
本项目正处于从“准一维开环”向“二维连续闭环”演进的关键节点：

* **阶段一：离散开环（已完成）**
  * 建立基础势场映射：输入坐标 $\rightarrow$ 物理建模 $\rightarrow$ 执行输出。
* **阶段二：一维闭环反馈（当前攻坚）**
  * 引入实时视觉误差补偿，建立闭环反馈系统，克服氧化层演变带来的非线性摩擦力干扰。
* **阶段三：二维矢量合成（未来资产）**
  * 对平板电极进行空间离散化，合成指向目标坐标的合力矢量 $\vec{F}_{total}$，实现复杂轨迹运动。

## 5. 部署与复现 (Deployment & Reproduction)
*详细的环境搭建（Conda）、硬件拓扑连线及 Streamlit 调参界面的使用说明。*
*(此处留白，待后续填入具体的命令行和连线图)*

## 6. 收获与反思 (Gains & Reflections)
* **物理模型的不确定性**：开环控制的失效让我深刻认识到液态金属非定常动力学过程的复杂性。反馈机制的引入是将理论公式拉回现实物理世界的必要“锚点”。
* **架构解耦的价值**：通过构建可视化的调参前端，成功将“工程架构设计”与“参数敏感度分析”解耦，极大提高了科研效率与系统的鲁棒性。