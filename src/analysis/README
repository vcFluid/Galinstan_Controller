# 🧠 Galinstan Physics-Informed Brain (PhysicsBrain)

## 1. 定位与使命 (Mission)
`Brain.py` 是本项目[ Galinstan 液态金属控制引擎 ]的**逻辑中枢**。其核心任务是执行从“空间误差场”到“电驱动力场”的非线性映射。通过将经典的 PID 控制律与液态金属的电毛细物理模型相结合，实现对液滴位置的精准调控。

### “空间误差场”（Space Error Field）并不是一个虚玄的修辞，而是一个极其硬核的**坐标映射逻辑**。

简单来说，**“空间误差场** 是指在视觉传感器（摄像头）所覆盖的二维物理空间内，每一个点相对于“目标位置”的偏离矢量的集合。

---

#### 1.1. 物理本质：从“点”到“场”
在基础编程中，我们通常只考虑一个数字：`error = target - current`。但当以“场”的思维去观察时，逻辑会发生质变：

* **标量场视角**：想象 640×480 像素平面是一张地形图，目标点 `TARGET_X` 是山谷的最低点（势能为 0）。 空间中任何其他坐标 $(x, y)$ 都拥有一个“势能值”，这个值的大小就是它距离目标的几何距离。
* **矢量场视角**：空间中的每一个坐标点都自带一个箭头，箭头指向目标点，箭头的长度代表误差的大小。

在 `Brain.py` 中，虽然目前只处理 1D 的 X 轴偏离，但实际上是在处理这个场在 X 方向上的投影：
$$e(x) = x_{target} - x_{current}$$

---

#### 1.2. 为什么称之为“场”？（离散化的必然）
brain.py程序的实现难点在于物理意义离散化为代码。之所以称为“场”，是因为：

1.  **全域性**：无论液滴出现在摄像头的左上角还是右下角，`Brain.py` 都能通过当前的坐标计算出对应的驱动指令。这说明控制逻辑覆盖了整个视觉探测区域。
2.  **连续性（理想化）**：虽然像素是离散的，但在物理建模时，我们假设液滴在空间中的移动是连续的。误差的变化也应当是平滑的梯度。

---

#### 1.3. 在项目中如何表现
在 `Brain.py` 的 `think` 方法中，执行的操作本质上是：
**输入一个空间位置 $\to$ 查询该位置在“空间误差场”中的值 $\to$ 映射为“驱动力场”中的电压。**

* **输入**：`current_pos`（液滴在场中的当前坐标）。
* **处理**：计算该点在场中的“斜率”（通过 PID 的 $D$ 项）。
* **输出**：`V_ideal`（为了抵消这个误差，需要在这个点施加多大的电场力）。

---

### 1.4. 这种思维的“反内卷”价值
如果只把 `error` 看作一个数字，那么只能做一个简单的跟随器。但如果把它看作“误差场”：
* **路径规划**：可以预设液滴绕过障碍物的“势能路径”。
* **多点协同**：如果将来控制多个液滴，会发现它们其实是在同一个“误差场”里互相干涉。

## 2. 核心演算逻辑 (Core Logic)
系统采用“两阶段演化”策略：

### 阶段 A：动力学期望演算 (PID Controller)
利用离散化算子计算维持物理平衡所需的理想驱动力 $F_{ideal}$：
1. **比例项 (Proportional)**：响应即时位移偏差。
2. **积分项 (Integral)**：累积静差能，强制打破氧化膜静摩擦滞后，包含**抗饱和 (Anti-windup)** 逻辑。
3. **微分项 (Derivative)**：通过有限差分法预测趋势，提供系统阻尼，防止过冲。

### 阶段 B：物理反演映射 (Physical Inversion)
将抽象的“力”映射为真实的“输出电压” $V_{ideal}$。基于 **Lippmann 方程** 的二次相关性简化模型：
$$V_{ideal} = \text{sgn}(F_{ideal}) \cdot (\sqrt{k \cdot |F_{ideal}|} + V_{threshold})$$

---

## 3. 关键物理参数表 (Parameter Registry)

| 寄存器名称 | 物理意义 | 默认值 | 备注 |
| :--- | :--- | :--- | :--- |
| `Kp` | 系统刚度 (Stiffness) | `1.0` | 决定对误差的响应速度。 |
| `Ki` | 能量累积率 | `0.0` | 用于消除稳态误差。 |
| `Kd` | 阻尼系数 (Damping) | `0.0` | 吸收动能，抑制震荡。 |
| `Critical_V` | 破冰电压 (Dead-zone) | `1.2V` | 克服氧化层静摩擦所需的最小电压。 |
| `K_FACTOR` | 转换系数 | `0.5` | 无量纲系数，调节驱动力强度。 |

---

## 4. 离散化实现细节 (Implementation Details)
* **时钟驱动**：`think` 方法严格依赖于外部传入的高精度时间步长 `dt` 。
* **状态保护**：
* **积分限幅**：`np.clip(..., -50, 50)` 确保计算溢出不会导致物理失稳 。
* **尖峰抑制**：在 `update_target` 时强制清空状态寄存器，防止微分项因目标跳变产生电压尖峰 。

---

## 5. 开发者调试指南 (Tuning Guide)
1. **死区判定**：若液滴在小位移下完全不动，应优先标定 `Critical_V` 。
2. **抗震荡调优**：若液滴在目标点附近往复运动，应增大 `Kd` 或减小 `Kp` 。
3. **响应加速**：若液滴移动迟缓，无法克服阻力，应缓慢增加 `Ki` 或 `Kp` 。

---

## 6. 溯源与未来扩展 (Future Scope)
* **电化学修正**：目前的模型忽略了界面电荷积累效应，未来需引入一阶滞后环节。
* **自适应演化**：计划引入在线参数辨识，根据液滴的实际运动轨迹动态拟合 `k_factor`。

---

### 【PS】
*提示：这份 README 不仅仅是说明书，更是对系统物理边界的正式宣告。当在实验室观察到液滴出现非预期的非线性颤噪时，可以对照“阶段 B”的映射公式，思考是否是 `k_factor` 的线性假设在特定浓度电解质下已经失效*
